// Config.gs
//
// Environment constants for the AgencyViz GoHighLevel Looker connector.
// Real credentials live in the Apps Script project editor only.

var API_BASE = 'https://app.agencyviz.io';

// Issued by the AgencyViz OAuth backend. Registered in the oauth_clients
// table with a hashed secret. These are placeholders — replace in the
// Apps Script editor with real values.
var OAUTH_CLIENT_ID = 'REPLACE_WITH_GHL_CLIENT_ID';
var OAUTH_CLIENT_SECRET = 'REPLACE_WITH_GHL_CLIENT_SECRET';
